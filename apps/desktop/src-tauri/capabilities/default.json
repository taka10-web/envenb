{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core IPC only. No shell, fs, or clipboard plugins are exposed.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
